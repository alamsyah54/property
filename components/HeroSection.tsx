import React from "react";

const HeroSection = () => {
  return (
    <section id="hero" className="duration-500">
      <div className=" w-screen min-h-screen flex-center">
        <div className="flex-center w-full"></div>
      </div>
    </section>
  );
};

export default HeroSection;
